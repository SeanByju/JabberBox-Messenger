<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"><img src="/images/Chatterbox.png" alt="" class="img-responsive"> JabberBox Messenger</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <div class="navbar-nav ms-auto dropdown dropdown" onclick="event.stopPropagation()">
          <% if(currentUser){ %> 
            <a id="notifLink" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false"><i id="bell" class="bi bi-bell-fill"></i> Notifications</a>
            <ul id="notif" class="dropdown-menu p-3" style="overflow-y:auto; max-height:25vh">
              <small>No new notifications!</small>
            </ul>
            <a class="nav-link" href="/<%= currentUser._id %>/settings"><i class="bi bi-gear-fill"></i> User Settings</a>
          <% } %> 
          <% if(!currentUser){ %> 
            <a class="nav-link" href="/login">Login</a>
            <a class="nav-link" href="/register">Register</a>
          <% }else{ %> 
            <a class="nav-link" href="/logout">Logout</a>
          <% } %> 
        </div>
      </div>
    </div>
</nav>